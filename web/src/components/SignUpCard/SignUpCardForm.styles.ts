import { css } from 'linaria';

import { helper } from '../../_variables.styles';

export const root = css`
  place-self: end;
`;

export const buttonContainer = css`
  ${helper.rightAligned}
  margin: 1.5rem 0 1rem 1rem;
`;

export const textInput = css`
  margin-top: 0.5rem;
`;
